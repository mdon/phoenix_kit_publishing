# Follow-up Items for PR #5

Triaged against `main` on 2026-04-26.

## No findings

PINCER_REVIEW verdict: Approve. Docs-only PR adding the routing anti-pattern pointer to `AGENTS.md` (the `Critical Conventions` warning that plugin LiveView routes must never be hand-registered in a parent app's router). No blockers, no findings. Re-checked current `AGENTS.md` on `main` — pointer is still present at the `Admin routing` bullet in `## Critical Conventions`.

## Open

None.
